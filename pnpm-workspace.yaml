packages:
  - "packages/*"

# Postinstall scripts pnpm 11+ requires explicit approval for.
# These tools download platform-specific native binaries via
# postinstall — required for normal operation.
allowBuilds:
  '@biomejs/biome': true
  esbuild: true
