// Dummy LLM client — first-class shipped test double. Cycles through a
// scripted Array<LLMResponse | Error>, captures inspection state, runs
// the same event-rendering pass real providers do. Implementation lands
// in a later commit.

export {}
