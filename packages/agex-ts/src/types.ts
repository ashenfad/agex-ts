// Canonical type module — discriminated unions for events/emissions/tokens,
// the RuntimeAdapter and LLMClient contracts, registration record types,
// and the error hierarchy. Filled in by the next commit.

export {}
